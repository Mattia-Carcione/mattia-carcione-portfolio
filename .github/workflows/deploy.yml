name: Deploy

on:
  push:
    branches:
      - origin

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print API KEY
        run: echo

      - name: Set API key
        run: echo "window.API_KEY = '${{ secrets.API_KEY }}';" >> $GITHUB_WORKSPACE/assets/js/main.js
